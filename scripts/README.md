# Scripts

Automation scripts for exporting, deploying, or configuring Power Platform assets.

This folder is currently empty. When adding scripts:
- Use descriptive filenames (e.g., `export-solution.ps1`)
- Include inline comments explaining usage
- Document any prerequisites (e.g., Power Platform CLI, authentication)
